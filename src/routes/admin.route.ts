// import { approveDriver, deleteOneDriver, deleteOneUser, deleteOneVehicles, editOneDriver, editOneUser, editOneVehicle, getDrivers, getOneDriver, getOneUser, getOneVehicle, getUsers, getVehicles, rejectedDriver } from '../controllers/admin.controller';
import { Router } from 'express';

const router = Router();


// // User 
// router.get('/users', getUsers);
// router.get('/user/:userId', getOneUser);
// router.put('/user/:userId', editOneUser);
// router.delete('/user/:userId', deleteOneUser);

// // Driver
// router.get('/drivers', getDrivers);
// router.get('/driver/:driverId', getOneDriver);
// router.put('/driver/:driverId', editOneDriver);
// router.delete('/driver/:driverId', deleteOneDriver);

// router.patch('/drivers/:driverId/approve', approveDriver);
// router.patch('/drivers/:driverId/reject', rejectedDriver);

// // Vehicle 
// router.get('/vehicles', getVehicles);
// router.get('/vehicle/:vehicleId', getOneVehicle);
// router.put('/vehicle/:vehicleId', editOneVehicle);
// // router.delete('/vehicle/:vehicleId', deleteOneVehicles);

export default router;